export type bugProps = {
    id: string;
    description: string;
    fixed: boolean;
    date: string;
    title: string;
    // url?: string;
    // deleteBug?: (id:number) => void;
    // updateBugTitle?: () => void;
};

